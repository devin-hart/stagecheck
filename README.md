# Stage Check tool reviews all pages, adding a URL. The user pushes a URL and clicks on "Run audit," and the page is fully reviewed.
# It checks the SEO, accessibility, and layout of the page and presents a report with items to fix.
# See in /crawler/playwright.js everything that is checked. More criteria to be added soon.  
